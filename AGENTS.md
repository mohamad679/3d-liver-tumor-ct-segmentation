# Agent Instructions

- Work on exactly one named phase at a time.
- Read `PROJECT_SPEC.md`, `ACCEPTANCE_CHECKLIST.md`, and `docs/PHASE_PLAN.md` before editing.
- Inspect Git status before work.
- Do not fabricate metrics, command results, dataset availability, or GPU runs.
- Do not commit medical data, datasets, model weights, predictions, credentials, API keys, or large generated artifacts.
- LiTS and MSD Task03 Liver must not be treated as independent cohorts.
- External labels must never be used for tuning or model selection.
- Request approval before destructive commands, system changes, large downloads, or long GPU runs.
- Every nontrivial implementation requires type hints, docstrings, explicit errors, and tests.
- End every phase with changed files, commands, tests, scientific decisions, risks, gate status, and next-phase prompt.
