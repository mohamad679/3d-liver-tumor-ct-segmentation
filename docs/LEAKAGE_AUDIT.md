# Leakage Audit

## Phase 2 Audit Status

Status: TEMPLATE ONLY — NOT EXECUTED

This template defines the evidence required to close Gate 2. Blank values are intentional. Do not
infer dataset availability, case counts, successful overlap checks, or audit passage from this
document. Gate 2 remains not evaluated until evidence is generated by the approved Phase 2 workflow
and reviewed.

## Reproducibility Evidence

- Source development manifest hash: `<not generated>`
- Split-policy name and version: `<not selected>`
- Split seed: `<not selected>`
- Split artifact hash: `<not generated>`
- QA artifact schema version: `<not generated>`
- QA artifact or index hash: `<not generated>`
- Git commit used for audit: `<not recorded>`
- Audit generation timestamp supplied explicitly: `<not recorded>`

## Partition Counts

| Partition | Patient count | Case count |
| --- | ---: | ---: |
| Train | `<not measured>` | `<not measured>` |
| Validation | `<not measured>` | `<not measured>` |
| Immutable internal test | `<not measured>` | `<not measured>` |
| Total unique | `<not measured>` | `<not measured>` |

## Pairwise Overlap Evidence

| Partition pair | Patient overlap count | Case overlap count |
| --- | ---: | ---: |
| Train / validation | `<not measured>` | `<not measured>` |
| Train / immutable internal test | `<not measured>` | `<not measured>` |
| Validation / immutable internal test | `<not measured>` | `<not measured>` |

Required outcome for every overlap cell: zero. Counts must be computed from saved manifest and split
artifacts, not entered from memory or estimated.

## Duplicate and Near-Duplicate Evidence

- Duplicate image-hash findings within the development cohort: `<not evaluated>`
- Duplicate label-hash findings within the development cohort: `<not evaluated>`
- Duplicate image-hash findings across development partitions: `<not evaluated>`
- Duplicate label-hash findings across development partitions: `<not evaluated>`
- Cross-partition near-duplicate policy and method: `<requires approval before execution>`
- Cross-partition near-duplicate findings: `<not evaluated>`

Exact SHA-256 duplication is a blocking finding until resolved. The cross-partition near-duplicate
method, threshold, review procedure, and false-positive handling must be specified and approved
before it is run; it may use development data only and must not compare against the real external
cohort during Phase 2.

## Cohort and Processing Confirmations

- LiTS/MSD equivalence warning reviewed: `<not signed>`

  LiTS labeled training data is the development cohort. MSD Task03 Liver is derived from LiTS and
  must never be counted, split, tuned, or reported as an independent cohort.

- Real 3D-IRCADb-01 external data was not accessed, scanned, inventoried, QA'd, split, tuned on, or
  summarized during Phase 2: `<not confirmed>`
- External labels did not influence development decisions: `<not confirmed>`
- No preprocessing was fitted on validation, immutable internal-test, or external data:
  `<not confirmed>`
- Split creation occurred before any data-dependent preprocessing fit: `<not confirmed>`
- Any lesion-aware stratification used only saved development QA artifacts and no external data:
  `<not applicable or not confirmed>`
- Every development patient and case was assigned exactly once: `<not confirmed>`
- Real-data inputs were supplied through an explicit lawful local root and outputs stayed beneath an
  explicit external generated root: `<not confirmed>`

## Unresolved Findings

| Severity | Finding | Evidence reference | Required resolution | Status |
| --- | --- | --- | --- | --- |
| `<not entered>` | `<not entered>` | `<not entered>` | `<not entered>` | `<open>` |

Gate 2 requires no unresolved critical finding. Findings must not be deleted after resolution;
retain them with their resolution evidence and final status.

## Review and Sign-Off

- Auditor/reviewer: `<not assigned>`
- Review date: `<not recorded>`
- Evidence reviewed: `<not recorded>`
- Unresolved critical findings: `<not evaluated>`
- Gate 2 leakage-audit recommendation: `<not evaluated>`
- Reviewer sign-off: `<not signed>`

No audit has passed at this stage.
